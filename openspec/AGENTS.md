# OpenSpec

Capability: `hardware-kit`. Changes live in `openspec/changes/<id>/`.
Fold updates `openspec/specs/hardware-kit/spec.md`.

This repo’s human-facing notes stay under `docs/`. Spec SHALLs live here.

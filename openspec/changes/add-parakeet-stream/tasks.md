# Tasks

- [ ] Stream route on the Hono app (chunked POST or WS; one, named in OpenAPI)
- [ ] Windowed TDT decode on the sidecar
- [ ] NDJSON or SSE events `{text, t0, t1}`
- [ ] Well-known spec lists the stream operation
- [ ] GPU idle during a 30 s pipe

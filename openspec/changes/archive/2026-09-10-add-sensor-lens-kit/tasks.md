# Tasks

- [x] Write `docs/SENSORS.md`
- [x] Delta `hardware-kit` sensor requirements
- SKU shopping is later (classes live in `docs/SENSORS.md`; /i glass preferred)
- [x] Amend the design and delta so HSB/USB is explicitly an AGX bring-up fallback; the production body path remains enclosure-mounted CSI/GMSL into Thor NVENC.
- [x] Amend the design and delta to separate the P-iris turret encode stub from the shipped satellite class, and specify the hardware path for Cooke-class lens metadata plus per-satellite UART ToF alongside edge-encoded PoE H.265.

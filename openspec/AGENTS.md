# OpenSpec

Capabilities: `hardware-kit`, `cartridge-runtime`. Changes live in
`openspec/changes/<id>/`. Fold updates `openspec/specs/<cap>/spec.md`.

This repo’s human-facing notes stay under `docs/`. Spec SHALLs live here.
The kit crib (SKUs, BOM, quotes) is **daBOM** (`kit-prod` / `kit-lab` /
`kit-hybrid-plant`), not a parts list in this spec.
